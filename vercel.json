{
  "version": 2,
  "buildCommand": "npm run build && esbuild server/server-wrapper.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/server-wrapper.js",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
